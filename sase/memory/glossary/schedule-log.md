---
keyword: Schedule Log
---

When the `<ctrl+shift+p>` Obsidian keymap is used to add the `scheduled` or `priority` dataview property to a task (or when the `bob capture` command's input argument contains the special `p:<N>` syntax), we add a `SCHEDULE LOG` bullet that contains one sub-bullet that corresponds with each time the task was scheduled / re-scheduled.
